package com.sgpublic.scit.tool.api.manager

class SignManager {
    companion object {
        fun calculate(map: Map<String, Array<String>>){
            val sortedMap: Map<String, Array<String>> = map.toSortedMap()

        }

        private fun getDefaultAppKey(): String {
            TODO()
        }
    }
}